services:
  flashcardquiz:
    image: ghcr.io/luke-butcher/flashcardquiz:latest
    container_name: flashcardquiz
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/flashcards.db
